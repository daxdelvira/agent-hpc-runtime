# SUPERSEDED — do not edit

This tree was the pre-IEEE draft. `sc-workshop-paper/paper_ieee/` is the live
paper: it is built on the authoritative IEEEtran `main.tex`, and it is the only
tree receiving edits.

Files here are kept for diffing against the earlier structure. They do NOT
carry the redundancy cuts, the float reordering, or the corrected
`fig:tool-relationships` claim. `figures/` here is still refreshed by
`scripts/figures/make_figures.py` only because the installer writes to both
paths; that is convenience, not a signal that this tree is current.
