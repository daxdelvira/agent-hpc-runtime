% ===========================================================================
% STUB -- INTENTIONALLY EMPTY. This file contributes nothing to the compiled
% document until it is written.
%
% The authoritative main.tex \input's sections/05_conclusion as the last
% section before the bibliography, but no conclusion exists in the source
% draft (sc-workshop-paper/paper/ ends at the evaluation) and none was
% invented here.
%
% WHAT BELONGS HERE
%   \section{Conclusion}
%   followed by a short close. The draft's own material suggests the shape:
%   the finding that byte placement is the wrong unit of work for agentic
%   workflows, the two mechanisms and what each is structurally able to
%   address (retention cannot help a first use; staging cannot help without a
%   window), and the boundaries the evaluation already states explicitly --
%   the zero-eviction regime above roughly 86% of total footprint, the
%   short-window workload where the system is correctly indistinguishable
%   from the unmodified baseline, and the untested memory-bandwidth
%   contention regime flagged in the CPU-interference subsection.
%
%   Any future-work text should be consistent with the OPEN ITEM recorded in
%   the header of sections/04b_evaluation.tex (the exact-selection column of
%   04_accuracy_sweep.md).
%
% TODO: write the conclusion. Reuse only claims the body already supports; do
% not introduce a number that appears nowhere else in the paper.
% ===========================================================================
