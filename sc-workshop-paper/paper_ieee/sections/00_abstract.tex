% ===========================================================================
% STUB -- INTENTIONALLY EMPTY. This file contributes nothing to the compiled
% document until it is written.
%
% The authoritative main.tex \input's sections/00_abstract immediately after
% \maketitle, but no abstract exists in the source draft
% (sc-workshop-paper/paper/) and none was invented here.
%
% WHAT BELONGS HERE
%   \begin{abstract} ... \end{abstract}  (IEEEtran provides the environment;
%   it must appear after \maketitle and before the first \section, which is
%   exactly where this file is \input'ed).
%
%   Optionally, IEEE keywords immediately after the abstract:
%   \begin{IEEEkeywords} ... \end{IEEEkeywords}
%
%   Content should be condensed from sections/01_introduction.tex, which
%   already states the problem (agentic steering breaks the phase-predictable
%   assumption DMSH staging depends on), the two mechanisms (retention
%   arbitrator, opportunistic prefetcher) and the headline results. Every
%   number quoted must be traceable to the same source as the body text --
%   see the provenance header of sections/04b_evaluation.tex. Do not restate
%   a figure here that the body does not support.
%
% TODO: write the abstract. Do not copy numbers from memory; re-read them from
% the cells named in sections/04b_evaluation.tex.
% ===========================================================================
